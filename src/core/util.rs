pub fn max(x: f64, y: f64) -> f64 {
    if x > y {
        return x;
    } else {
        return y;
    }
}

pub fn min(x: f64, y: f64) -> f64 {
    if x > y {
        return y;
    } else {
        return x;
    }
}
